// Create function func with arguments
// the task condition is in the Readme file

import 'dart:math';

void main() {
  func(2);
}

double func(int a) {
  print((a / 2) * (((a + pow(a, 2)) / (pow(2, a)))));
  return 0;
}
