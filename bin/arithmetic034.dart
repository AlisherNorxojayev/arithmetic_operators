// Create function func with arguments
// the task condition is in the Readme file

import 'dart:math';

void main() {
  func(2);
}

double func(
  int a,
) {
  print(pow(2, a) + ((a + 7 * a) / 4));
  return 0;
}
