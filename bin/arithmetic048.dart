// Create function func with arguments
// the task condition is in the Readme file

import 'dart:math';

void main() {
  func(2, 4);
}

int func(int a, int b) {
  print(a * b - 2 * b + pow(a, 2));
  return 0;
}
